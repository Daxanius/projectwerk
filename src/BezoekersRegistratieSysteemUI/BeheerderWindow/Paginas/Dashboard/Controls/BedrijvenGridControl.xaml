<UserControl
    x:Class="BezoekersRegistratieSysteemUI.BeheerderWindowPaginas.Dashboard.Controls.BedrijvenGridControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BezoekersRegistratieSysteemUI.BeheerderWindowPaginas.Dashboard.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <StackPanel Margin="0,20,60,0">
        <Label
            Padding="0,10,0,20"
            Content="Dashboard"
            FontSize="40"
            FontWeight="Bold"
            Foreground="{StaticResource DonkerBlauw}" />
        <ScrollViewer
            MaxHeight="440"
            Padding="0,0,0,0"
            CanContentScroll="True"
            Foreground="{StaticResource DonkerBlauw}">
            <Grid Name="gridContainer">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
            </Grid>
        </ScrollViewer>
    </StackPanel>
</UserControl>
